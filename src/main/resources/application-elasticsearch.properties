# Enable Spring Data Elasticsearch repositories and Kafka → ES indexing.
# Use with a running cluster: docker compose up -d
#   mvn spring-boot:run -Dspring-boot.run.profiles=elasticsearch
spring.data.elasticsearch.repositories.enabled=true
